import Button from "./Button";
import Input from "./Input";

const Form = () => {
  return (
    <div className="flex">
      <Input />
      <Button title="Create" />
    </div>
  );
};

export default Form;
